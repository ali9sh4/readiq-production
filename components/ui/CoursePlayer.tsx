/**
 * @deprecated The course player no longer lives under components/ui/ (it is
 * an app feature, not a primitive). Import from
 * "@/components/player/CoursePlayer" instead. This re-export exists so
 * historical references keep resolving; remove once nothing imports it.
 */
export { default } from "@/components/player/CoursePlayer";
